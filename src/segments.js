

function segments(from, to) {
	let lineElm;

	lineElm = createLine(from, to);

	return (
	{
		id: ,
		elm: lineElm,
		positions:
		{
			x: ,
			y: ,
		},
		from: from,
		to: to,
//		nodeFrom: ,
//		nodeTo: ,
		upDate: function (from, to)
		{

		}
	});
}
